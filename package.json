{
  "name": "movement-rate-limiter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test tests/*.test.js",
    "test": "node --test tests/*.test.js"
  }
}
